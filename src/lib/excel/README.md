# excel/

Generación del Excel semanal. Se implementa en **Fase 10**, usando ExcelJS sobre una plantilla `.xlsx` de la empresa (a ubicar en `supabase/storage` o `src/lib/excel/templates/` según se defina en esa fase).

Correrá exclusivamente server-side (route handler o Edge Function), nunca en el navegador.
